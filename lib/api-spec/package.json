{
  "name": "@workspace/api-spec",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "codegen": "orval --config ./orval.config.ts && node -e \"const fs=require('fs'),p='../api-zod/src/index.ts';fs.writeFileSync(p,fs.readFileSync(p,'utf8').split('\\n').filter(l=>!l.includes(\\\"./generated/types\\\")).join('\\n'))\" && pnpm -w run typecheck:libs"
  },
  "devDependencies": {
    "orval": "^8.9.1"
  }
}
